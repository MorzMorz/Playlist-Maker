<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Активное состояние (включен) -->
    <item android:color="@color/thumb_tint_active" android:state_checked="true" />
    <!-- Неактивное состояние (выключен) -->
    <item android:color="@color/thumb_tint_inactive" android:state_checked="false" />

</selector>